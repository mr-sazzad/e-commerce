export enum tagTypes {
  user = "user",
  cart = "cart",
  watches = "watches",
  payment = "payment",
}

export const tagTypeLists = [
  tagTypes.user,
  tagTypes.cart,
  tagTypes.watches,
  tagTypes.payment,
];
